---
import HitCounter from './HitCounter.astro';
import Webring from './Webring.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
const postsCount = posts.length;
---

<footer class="retro-footer">
	<hr class="footer-separator" />
	
	<div class="footer-widgets">
		<div class="footer-widget-item">
			<Webring />
		</div>
		<div class="footer-widget-item">
			<HitCounter start={84102} />
		</div>
	</div>

	<div class="footer-text-links">
		<a href={import.meta.env.BASE_URL}>Home</a> | 
		<a href={`${import.meta.env.BASE_URL}blog`}>Blog</a> | 
		<a href={`${import.meta.env.BASE_URL}interests`}>Interests</a> | 
		<a href={`${import.meta.env.BASE_URL}about`}>About</a> | 
		<a href={`${import.meta.env.BASE_URL}rss.xml`} target="_blank">RSS</a> | 
		<a href="#top">Back to Top ↑</a>
	</div>

	<div class="footer-copyright">
		<p class="copyright-line">Designed & Maintain'd by Biagio Scaglia | 2004 - 2026</p>
		<p class="best-viewed-line">Best viewed with 1024x768 resolution on Netscape Navigator 6.0 or IE 5.5+</p>
		<p class="disclaimer-line">No cookies were harmed in the making of this personal database.</p>
		<p class="status-line" style="font-family: 'Courier New', Courier, monospace; font-size: 10px; color: #008000; margin-top: 6px; font-weight: bold;">
			NODE STATUS: [ ONLINE ] | DB VERSION: v1.0.4 | TOTAL ENTRIES: {postsCount}
		</p>
	</div>
</footer>

<style lang="scss">
	@use "../styles/variables" as *;

	.retro-footer {
		background: $win-gray;
		@include win95-border-outset;
		padding: 15px;
		margin-top: 20px;
		text-align: center;
		font-family: $font-verdana;
	}

	.footer-separator {
		border: none;
		height: 2px;
		@include win95-border-inset;
		margin-bottom: 15px;
	}

	.footer-widgets {
		display: flex;
		justify-content: space-around;
		align-items: center;
		flex-wrap: wrap;
		gap: 15px;
		margin-bottom: 15px;
	}

	.footer-widget-item {
		flex-shrink: 0;
	}

	.footer-text-links {
		font-family: $font-tahoma;
		font-size: 11px;
		font-weight: bold;
		margin-bottom: 12px;

		a {
			color: #0000ee;
			text-decoration: none;
			margin: 0 4px;

			&:hover {
				color: #ff0000;
				text-decoration: underline;
			}
		}
	}

	.footer-copyright {
		font-size: 10px;
		color: #333333;
		line-height: 1.5;

		p {
			margin: 2px 0;
		}
	}

	.copyright-line {
		font-weight: bold;
		color: $win-blue;
	}

	.best-viewed-line {
		font-style: italic;
	}

	.disclaimer-line {
		color: $win-dark-gray;
	}
</style>
